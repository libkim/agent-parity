---
created: 2026-07-20T01:59:52.633915041Z
tags:
    - agent-parity
    - governance
    - cache-gc
    - prewarm
    - checkpoint
strength: 1
lastAccessed: 2026-07-20T01:59:52.633915041Z
---
캐시 GC + pre-warm·안내 구현 완료(2026-07-20, main 커밋, 다음 릴리스에 실림) — 거버넌스 후속 구현 전부 완료. ① 캐시 GC: install/update가 수렴 마지막(tombstone 뒤)에 memory-mcp·config 두 캐시 계열에서 현재 버전 외 디렉터리를 삭제 시도, 잠김 실패는 무시하고 다음 실행에 재시도, 지운 게 있을 때만 "cache: pruned N" 출력. ② pre-warm: run.sh/run.cmd에 prewarm 모드 추가(캐시 보장 후 서버 실행 없이 exit 0, 다운로드 로직 중복 없음) — self-heal이 세션 시작마다 호출해 GC로 지워졌거나 프레시 pull인 바이너리를 미리 받아옴. ③ 안내: 다운로드까지 실패하면 self-heal이 "memory tools may be offline... check network and restart" 출력, 항상 exit 0(Antigravity crash 방지). 겸사겸사 기존 조용한 실패도 경화: config editor 다운로드 실패 시 self-heal이 중간에 non-zero로 죽던 것을 notice+exit 0으로 변경. 실측 4경로: GC(구버전 2개 prune·현재 유지), prewarm(exit 0·캐시 채움), self-heal 성공(무음·복구), self-heal 실패(안내·exit 0). ps1 쌍은 파서 검증. 변경 파일 8개: run.sh·run.cmd·templates/self-heal.{sh,ps1}·install/update {sh,ps1}.
