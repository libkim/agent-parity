---
created: 2026-07-19T16:31:01.5793902Z
tags:
    - agent-parity
    - cursor-cli
    - config-editor
    - checkpoint
strength: 6
lastAccessed: 2026-07-20T15:43:22.6890574Z
---
agent-parity checkpoint: Cursor CLI registration is now symmetric through agent-parity-config. Added exact-path `merge-cursor-cli`, and install.sh/install.ps1/update.sh/update.ps1 call it instead of writing a whole `.cursor/cli.json` or asking for manual merge. It creates a missing file, appends only `Mcp(memory:*)` under `permissions.allow`, preserves unrelated settings and permissions, is idempotent, and rejects incompatible JSON shapes without rewriting. Status remains `has-cursor-cli`; uninstall remains `unmerge-cursor-cli`. Linux configeditor/default Go tests, shell/PowerShell syntax checks, release build, and full Windows configeditor test binary all passed.
