---
created: 2026-07-20T01:50:26.092360564Z
tags:
    - agent-parity
    - governance
    - tombstone
    - checkpoint
strength: 1
lastAccessed: 2026-07-20T01:50:26.092360564Z
---
매니페스트 차분 정리 구현 완료(2026-07-20, main 커밋, 다음 릴리스에 실림). install/update 4개 스크립트(sh·ps1 쌍)가 ARTIFACTS 옆에 TOMBSTONES 목록을 선언(은퇴 릴리스 주석 포함: dist/·agent-parity.ps1 둘 다 v0.6.0에서 은퇴)하고, 흩어져 있던 인라인 rm을 제거한 뒤 수렴 마지막에 remove_tombstones가 목록을 순회 삭제한다. 차분(구버전 산출물 합집합−현재 산출물)은 작성 시점에 계산해 목록으로 선언하는 형태 — 런타임 집합 연산은 불필요하다고 판단. 존재할 때만 "legacy: removed <path>" 출력. 실측: 레거시 2건 심고 update로 제거·멱등 확인, ps1은 Windows PowerShell 파서로 구문 검증. 거버넌스 잔여 구현: 캐시 GC, 바이너리 부재 pre-warm·안내.
