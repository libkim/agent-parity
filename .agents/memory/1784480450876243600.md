---
created: 2026-07-19T17:00:50.8762436Z
tags:
    - agent-parity
    - powershell
    - atomic-write
    - user-files
    - checkpoint
strength: 3
lastAccessed: 2026-07-20T15:43:22.6890574Z
---
agent-parity checkpoint: templates/common.ps1 Write-Text now uses a same-directory GUID staging file created with FileMode.CreateNew, writes the complete UTF-8 text to staging, then replaces the destination with Move-Item -Force; finally removes staging on every failure. This aligns local PowerShell user-file writes (notably AGENTS.md and .gitignore during uninstall) with existing install/update atomic-write behavior. Added test_atomic_write.ps1: successful replacement is verified, then an exclusive lock forces replacement failure and proves original content remains unchanged and no staging file remains. PowerShell AST plus atomic, marker, and Claude-wrapper runtime tests passed. Current installed project outputs and root .gitignore were not modified.
