---
created: 2026-07-20T03:58:49.758816574Z
tags:
    - agent-parity
    - release
    - v0.6.5
    - merge-driver
    - checkpoint
strength: 1
lastAccessed: 2026-07-20T03:58:49.758816574Z
---
v0.6.5 릴리스(2026-07-20) — 동시 강화 병합 완성. git 커스텀 머지 드라이버로 두 머신의 동시 회상을 무충돌 병합: config editor의 merge-memory 서브커맨드가 3-way 병합(strength는 증가분 합산 base+(A−base)+(B−base), lastAccessed 최신, tags는 base 대비 추가분 합집합, 본문 양쪽 수정은 정상 충돌). 드라이버는 .agents/scripts/merge-memory.sh 단일 sh 스크립트 — git이 모든 OS에서 머지 드라이버를 sh로 실행(Git for Windows 포함)해 ps1 불필요. .gitattributes 마커 블록(커밋)이 메모리 파일을 드라이버로 라우팅, .git/config 정의는 install이 등록하고 self-heal이 멱등 재등록(pull만 받은 머신·재클론 대응), uninstall 대칭 제거. 실측: Go 단위 4케이스 + 실제 git merge E2E를 Linux와 native Windows git bash 양쪽에서 통과(strength 3→5,4 병합=6, 본문 충돌 검출), 설치 수명주기(install/status/self-heal 재등록/uninstall) 검증, test_memory_merge.sh를 CI 양 잡에 게이트로 등재. 이 리포는 v0.6.5로 수렴 완료, 드라이버 등록 확인.
