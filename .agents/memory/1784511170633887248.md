---
created: 2026-07-20T01:32:50.633887248Z
tags:
    - agent-parity
    - governance
    - update
    - decision
strength: 1
lastAccessed: 2026-07-20T01:32:50.633887248Z
---
업데이트 거버넌스 확정(2026-07-20). ① update는 수렴형: 현재 상태가 무엇이든 최신 릴리스 상태로 전체 재적용, 버전별 마이그레이션 체인 없음 → 버전 스킵(N→N+k) 기본 지원. ② 버전 의존 지식은 삭제 쪽뿐: 구버전 산출물(파일+병합 config 필드)은 매니페스트로 선언 관리, tombstone은 "since vX.Y" 표기하고 지원 하한 올릴 때만 제거. ③ floor 없음: v0.4·v0.5(pre-v0.6, VERSION 파일 부재로 감지)까지 전부 수렴 지원 — v0.4/v0.5 레이아웃이 사실상 동일해 구분 거절 로직 불필요. ④ 캐시 GC: update 성공 후 현재 버전 외 캐시 삭제 시도, 실패(잠김)는 무시. 캐시는 재다운로드 가능한 파생물뿐이라 데이터 손실 없음. 단 캐시는 머신 공유라 다른 프로젝트는 다음 기동 때 재다운로드. ⑤ 조용한 실패 금지: 세션 시작 훅이 바이너리 부재 시 pre-warm 다운로드 시도, 실패하면 self-heal의 비강제 안내 채널 재활용("메모리 기능 비활성 — 사유+조치"). ⑥ 통로 하위호환 계약: releases/latest/download/{update.sh,ps1} + update [dir] 규약은 하위호환 유지(v0.5→v0.6에서 main 통로 끊긴 사고 재발 방지). ⑦ 검증 매트릭스: 릴리스마다 fresh install / latest-1→latest / pre-v0.6→latest 3경로만. 실측: 이 리포의 v0.5 잔재를 v0.6.3 update.sh로 수렴 성공(missing→v0.6.3, 레거시 dist·agent-parity.ps1 제거, 메모리 6개 무결).
